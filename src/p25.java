public class p25 {
    public static int[] buildArray(int size) {
        int[] done = new int[size];
        for (int i = 0; i < done.length; i++) {
            done[i] = size;
        }
        return done;
    }
}
